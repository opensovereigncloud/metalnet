---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: virtualips.networking.metalnet.onmetal.de
spec:
  group: networking.metalnet.onmetal.de
  names:
    kind: VirtualIP
    listKind: VirtualIPList
    plural: virtualips
    singular: virtualip
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualIP is the Schema for the virtualips API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of VirtualIP.
            properties:
              ip:
                description: IP is the allocated IP
                type: string
              ipFamily:
                description: IPFamily is the ip family of the VirtualIP.
                type: string
              targetRef:
                description: TargetRef references the target for this VirtualIP (currently
                  only NetworkInterface).
                properties:
                  name:
                    description: Name is the name of the referenced entity.
                    type: string
                  uid:
                    description: UID is the UID of the referenced entity.
                    type: string
                required:
                - name
                type: object
              type:
                description: Type is the type of VirtualIP.
                type: string
            required:
            - ipFamily
            - type
            type: object
          status:
            description: Status defines the observed state of VirtualIP.
            properties:
              machineID:
                description: 'TODO: we should establish a model how we can identify
                  a VirtualIP standalone'
                type: string
              phase:
                description: Phase is the VirtualIPPhase of the VirtualIP.
                type: string
              phaseLastTransitionTime:
                description: LastPhaseTransitionTime is the last time the Phase transitioned
                  from one value to another.
                format: date-time
                type: string
              underlayIP:
                description: UnderlayIP of the host
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
