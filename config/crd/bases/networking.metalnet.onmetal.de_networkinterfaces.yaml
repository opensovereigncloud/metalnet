---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: networkinterfaces.networking.metalnet.onmetal.de
spec:
  group: networking.metalnet.onmetal.de
  names:
    kind: NetworkInterface
    listKind: NetworkInterfaceList
    plural: networkinterfaces
    singular: networkinterface
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkInterface is the Schema for the networkinterfaces API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of NetworkInterface.
            properties:
              ip:
                description: IP is the provided IP or EphemeralIP which should be
                  assigned to this NetworkInterface
                type: string
              ipFamilies:
                description: IPFamilies defines which IPFamilies this NetworkInterface
                  is supporting
                items:
                  description: IPFamily represents the IP Family (IPv4 or IPv6). This
                    type is used to express the family of an IP expressed by a type
                    (e.g. service.spec.ipFamilies).
                  type: string
                type: array
              networkFn:
                description: NetworkFunction specifies the network function that should
                  be assigned to this NetworkInterface.
                properties:
                  networkFuncionRef:
                    description: VirtualIPRef references a VirtualIP to use.
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                type: object
              networkRef:
                description: NetworkRef is the Network this NetworkInterface is connected
                  to
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              nodeName:
                description: NodeName is the name of the host machine on which the
                  VM should be scheduled. If it is non-empty then scheduler simply
                  schedules the VM on that host assuming it has enough resources.
                type: string
              virtualIp:
                description: VirtualIP specifies the virtual ip that should be assigned
                  to this NetworkInterface.
                properties:
                  virtualIPRef:
                    description: VirtualIPRef references a VirtualIP to use.
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                type: object
              virtualMachineRef:
                description: VirtualMachineRef is the VirtualMachine this NetworkInterface
                  is used by
                properties:
                  name:
                    description: Name is the name of the referenced entity.
                    type: string
                  uid:
                    description: UID is the UID of the referenced entity.
                    type: string
                required:
                - name
                type: object
            required:
            - ipFamilies
            - networkRef
            type: object
          status:
            description: Status defines the observed state of NetworkInterface.
            properties:
              access:
                description: Access
                properties:
                  networkAttributes:
                    additionalProperties:
                      type: string
                    type: object
                  uid:
                    description: UID is the UID of network object
                    type: string
                required:
                - uid
                type: object
              ips:
                description: IPs represent the effective IP addresses of the NetworkInterface
                items:
                  type: string
                type: array
              phase:
                description: Phase is the NetworkInterfacePhase of the NetworkInterface.
                type: string
              phaseLastTransitionTime:
                description: LastPhaseTransitionTime is the last time the Phase transitioned
                  from one value to another.
                format: date-time
                type: string
              state:
                description: State is the NetworkInterfacePhase of the NetworkInterface.
                type: string
              virtualIP:
                description: VirtualIP is any virtual ip assigned to the NetworkInterface.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
